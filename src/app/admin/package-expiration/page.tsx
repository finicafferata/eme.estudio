'use client'

import { PackageExpirationManager } from '@/components/ui/package-expiration-manager'

export default function PackageExpirationPage() {
  return (
    <div className="container mx-auto p-6">
      <PackageExpirationManager />
    </div>
  )
}